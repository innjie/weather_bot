# 개발 가이드

## 1. 코딩규칙
- 파일은 가능한 모듈와, 재사용할 수 있는 방향으로
- 하드코딩은 최소화
- null 값 역참조 등 secure coding 준수하기


## 2. DB
### guild_configs
- 길드(서버)별 날씨 알림 설정 1건을 저장 (`guild_id` unique)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | Long (PK, autoincrement) | |
| guild_id | Long, unique | Discord 길드 ID |
| channel_id | Long | 알림 보낼 채널 ID |
| location | Varchar(100) | 날씨 조회 지역명 |
| weather_provider | Varchar(20) | `KMA` 또는 `OWM` |
| notify_hour | Int | 알림 발송 시(0~23) |
| notify_minute | Int | 알림 발송 분(0~59) |
| created_at / updated_at | Instant | 감사용 타임스탬프 |

정의 위치: `src/main/kotlin/com/weatherbot/db/GuildConfigTable.kt`

## 3. TO-DO
- [ ] Discord 봇 토큰 발급 및 `.env` 설정, 실제 로그인 테스트
- [x] DB 스키마 설계 (서버/사용자별 알림 위치, 알림 시각, 사용할 `WeatherProvider` 설정) 및 Exposed 테이블 정의
- [x] `DatabaseFactory` 구현 (SQLite 연결/초기화, 테이블 생성)
- [ ] `WeatherProvider` 구현체 - 기상청(단기예보) + 에어코리아(미세먼지) 연동
- [ ] `WeatherProvider` 구현체 - OpenWeatherMap 연동
- [ ] 설정 커맨드 구현 (알림 받을 위치, 알림 시각, 날씨 데이터 소스 선택)
- [ ] "오늘의 날씨 요약" 커맨드 및 자동 알림 메시지 포맷 구현
- [ ] 일별 상세 정보(최저/최고기온, 평균습도, 미세먼지 농도, 오전/오후 날씨) 포맷 구현
- [ ] 주간날씨 요약 커맨드 구현
- [ ] 다른 도시 날씨 조회 커맨드 구현
- [ ] 코루틴 기반 스케줄러 구현 (사용자별 지정 시각에 자동 알림 발송)
- [ ] 커맨드 실행 근거를 한국어로 안내하는 응답 로직 반영 (`claude.md` 작업 방식 2번)
- [ ] 예외/null 처리 등 secure coding 점검 (외부 API 실패, 잘못된 위치 입력 등)
- [ ] Docker 빌드 및 `docker-compose up`으로 상시 구동 검증